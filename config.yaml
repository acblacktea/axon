exchanges:
  deribit:
    env: testnet  # testnet / production
    api_key: "8W9wYEn_"
    api_secret: "lPkxAqgVifyJsQCIAOK-dses39KJeM-lmN_oSeKPzC0"

reconciliation:
  enabled: true
  interval_seconds: 300

websocket:
  heartbeat_interval_seconds: 10
  reconnect_delay_seconds: 1
  max_reconnect_delay_seconds: 60

portfolio:
  currencies: ["BTC"]
  position_refresh_interval_seconds: 1

zmq:
  router_endpoint: "tcp://*:5555"
  pub_endpoint: "tcp://*:5556"
  router_connect: "tcp://localhost:5555"
  pub_connect: "tcp://localhost:5556"

database:
  dsn: "postgresql://localhost:5432/calais"
  pool_min: 2
  pool_max: 10
